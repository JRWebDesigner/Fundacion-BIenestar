---
// Importa los íconos directamente (opcional, si usas una CDN o SVG)
const isPlaying = false; // Estado simple (sin React)
---

<div class="audio-player">
  <button id="audio-button" class='p-5 rounded-full bg-secondary'>
    <span id="audio-icon">🔇</span> <!-- Icono inicial -->
  </button>

  <audio id="bg-audio" loop>
    <source src="/audio.mpeg" type="audio/mpeg" />
  </audio>
</div>

<script>
  const button = document.getElementById('audio-button');
  const audio = document.getElementById('bg-audio');
  const icon = document.getElementById('audio-icon');

  let isPlaying = false;

  button.addEventListener('click', () => {
    if (isPlaying) {
      audio.pause();
      icon.textContent = '🔇';
    } else {
      audio.play().catch(e => console.error("Error al reproducir:", e));
      icon.textContent = '🔊';
    }
    isPlaying = !isPlaying;
  });
</script>

<style>
  .audio-player {
    position: fixed;
    bottom: 20px;
    right: 20px;
  }
  button {
    border: none;
    cursor: pointer;
    font-size: 24px;
  }
</style>
